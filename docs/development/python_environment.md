# Python Environment

The project uses a Python virtual environment to isolate backend dependencies.

The environment is created using:

```
python3 -m venv .venv
```

Dependencies are installed from:

```
backend/requirements.txt
```

Using a virtual environment prevents conflicts with system-wide Python packages and ensures consistent dependency management.